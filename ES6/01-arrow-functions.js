// Funcion normal
function square(num) {
    return num * num;
}

// Funcion flecha para multiples parametros
const square = (num) => {
    return num * num;
}

// Funcion flecha con return implicito
const square = num => num * num;

square(4);